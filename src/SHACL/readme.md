`schemaSHACL.ttl` defines SHACL validaiton rules CTDL schema files.  It does not handle the "with meta property" versions of the schema vocabulary definition files.

`TestFiles/` contains the CTDL schema vocab definition files at the time of writing, slightly modified so that they pass validation.